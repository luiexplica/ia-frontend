import { AbstractControl, ValidationErrors, ValidatorFn, Validators } from "@angular/forms";
import { ValidationRule_I } from "../interfaces";
/*
export const get_Validation = (rule: ValidationRule_I) => {

  if (rule.type === 'required') {
    // return formgroup validation required


  }
  if (rule.type === 'notRequired') {
  }
  if (rule.type === 'minLength') {
  }
  if (rule.type === 'maxLength') {
  }
  if (rule.type === 'email') {
  }
  if (rule.type === 'tel') {
  }
  if (rule.type === 'url') {
  }
  if (rule.type === 'pattern') {
  }
  if (rule.type === 'same_field') {
  }
  if (rule.type === 'conditional_required') {
  }
  if (rule.type === 'conditional_select_single_required') {
  }
  if (rule.type === 'conditional_select_multi_required') {
  }
  if (rule.type === 'fileFormat_document') {
  }
  if (rule.type === 'fileFormat_image') {
  }
  if (rule.type === 'fileFormat_video') {
  }
  if (rule.type === 'fileSize_5m') {
  }
  if (rule.type === 'fileSize_10m') {
  }
  if (rule.type === 'select_single_required') {
  }
  if (rule.type === 'select_multi_required') {
  }
  if (rule.type === 'isArray_required') {
  }

} */
